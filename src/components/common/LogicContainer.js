import React, { Component } from "react";
import { Text, StyleSheet, View } from "react-native";

export default class LogicContainer extends Component {
    render() {
        return (
            <View>
                <Text> LogicContainer.js </Text>
            </View>
        );
    }
}

const styles = StyleSheet.create({});
